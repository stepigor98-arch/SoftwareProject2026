# Entry point: starts the Tkinter app (creates App and runs mainloop).


# Loading the main application module
from app import App


# Entry point for the application
if __name__ == "__main__":
    App().mainloop()